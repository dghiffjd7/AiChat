# OpenCode FC 候选模型批量验收报告（2026-08-15）

## 范围与结果

- 目录指纹：`sha256:f6fc895cc1b0f84aa82fc2158fdc6b3b8d28201b4f0c9b40adcc515cb7a7c409`
- OpenCode Go 目录共 26 个模型；现有 Chat Completions adapter 匹配 16 个。
- `glm-5.2`、`glm-5.3` 已有发布证据，本轮不重复计费；其余 14 个候选全部进入分层矩阵。
- 实际模型请求共 148 次，无中断或未决步骤。4 个模型通过 transport，且严格 surface 样本合计 `120/120`；最终 2 个模型完整通过 release。
- 所有真实会话（含失败后 fallback）均完成临时会话、联系人、记忆与运行时清理，没有持久测试数据残留。

| 模型 | Transport | 私聊／群聊／动态 | 取消边界 | 临时真实会话 | 结论 |
|---|---|---|---|---|---|
| `glm-5` | `2/2` | `30/30` | 通过 | 通过，1 次请求、0 fallback | 已加入 bundled revision 3 |
| `glm-5.1` | `2/2` | `30/30` | 通过 | `no_tool_call`，随后文本 fallback | 不放行 |
| `mimo-v2.5` | `2/2` | `30/30` | 通过 | `no_tool_call`，随后文本 fallback | 不放行 |
| `mimo-v2.5-pro` | `2/2` | `30/30` | 通过 | 通过，1 次请求、0 fallback | 已加入 bundled revision 3 |

## Transport 阶段淘汰

| 模型 | 结果 |
|---|---|
| `deepseek-v4-flash`、`deepseek-v4-pro` | 首个流式具名工具请求返回 HTTP 400 |
| `hy3-preview` | 首个流式具名工具请求返回 HTTP 400 |
| `kimi-k2.5`、`kimi-k2.6`、`kimi-k2.7-code`、`kimi-k3` | 首个流式具名工具请求返回 HTTP 400 |
| `mimo-v2-omni`、`mimo-v2-pro` | 首个流式具名工具请求返回 HTTP 400 |
| `hy3` | 返回一个工具调用，但参数未满足严格 schema |

HTTP 400 的服务端正文依照矩阵隐私合同未写入报告，因此这里只能据请求状态判定当前适配组合不通过，不能据此推断模型永久不支持 FC。

## 调用分布

| 阶段 | 实际请求 |
|---|---:|
| Transport | 18 |
| 前 6 个 surface 样本 | 24 |
| 扩展至 30 个 surface 样本 | 96 |
| 取消边界与真实会话 | 10 |
| 合计 | 148 |

最后一层实测发现真实会话失败时会再发送一次文本 fallback，因此矩阵原先为该步骤预留 1 次不足。预算定义已修正为最多 2 次：单模型完整验收上限由 34 调整为 35；这不改变本报告的模型判定，也无需重跑既有证据。

## 发布结果

- `glm-5`、`mimo-v2.5-pro` 已作为精确规则加入 bundled revision 3；它们不会授权同系列、别名、Custom 或代理端点。
- `glm-5.1`、`mimo-v2.5` 不应因 30 个合成样本全过而放行；真实发送链已证明仍可能回退。
- 其余 10 个模型继续 fail-closed。若未来端点能力或适配方式更新，应使用新的报告文件重新跑矩阵，不覆盖本次证据。
